-->> suppose aapke page.js m koi data h eg. use state variable data m aapko ye data header component m bhjna h aur vhn se ye data nav compo m bhjna h
ab aapka data direct nav m jaane ki jgh header se hote hue jara h jo safe nahi h
kyuki nav direct page se nahi balki header se call hora to ham direct data page.js se nav m nahi bhej paare
yehi kaam direct karne k liye context api kaam aati h
data ek jgh store krte h isme aur phir sab vahin se use hogi.
small application(300-400 component) m context api aur bade app m redux toolkit use hota h is kaam k liye.

--> context bnana and use krna 
    we create a folder in the outermost scope for context , so that all can use and acess inside that create file Context.js
    and create and intialise context
 -> update layout.js
    import './globals.css'
    import MyContext  from '@/Helper/Context'
    export const metadata = {
    title: 'Create Next App',
    description: 'Generated by create next app',
    }

    export default function RootLayout({ children }) {
    return (
        <html lang="en">
        <body><MyContext>{children}</MyContext></body>
        </html>
    )
    }

-> context.js initialise
    "use client"
    import React, { createContext } from 'react'
    export const MyContext=createContext()
    const context = ({children}) => {
        const username="jay";
    return (
        <div>
        <MyContext.Provider value={username}>
            {children}
        </MyContext.Provider>
        </div>
    )
    }

    export default context

-> using context
    "use client"
    import React, { useContext, useState } from 'react'
    import Header from './Components/Header'
    import { MyContext } from '@/Helper/Context'
    const page = () => {
    const user= useContext(MyContext)
    return (
        <div>
        Home page {user}
        </div>
    )
    }

    export default page





*** object import {} k andr hote h baaki sab normal.